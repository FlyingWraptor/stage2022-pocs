import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tn-stage2022-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
